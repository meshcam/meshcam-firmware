# Trail-cam gateway — 8MB Heltec V3, OTA-capable (two app slots) for espota WiFi updates.
# nvs kept at the stock 0x9000/0x5000 offset so the gateway's persistent RNS identity
# (Preferences "gateway" namespace) survives this partition-table change untouched.
# app slots are 2.75MB each (fw is ~1.6MB, big headroom); spiffs holds the LittleFS
# spool + RNS stores. Replaces no_ota.csv (single-slot, could not OTA).
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x2C0000,
app1,     app,  ota_1,   0x2D0000, 0x2C0000,
spiffs,   data, spiffs,  0x590000, 0x260000,
coredump, data, coredump,0x7F0000, 0x10000,
